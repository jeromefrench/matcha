<% include elements/header %>

<div class="ui container">

	<h1>Research</h1>
	<%- include('elements/research_panel', {session: session}); %>

	<script>
		$('.ui.dropdown')
			.dropdown()
		;
	</script>
	<p>
	</p>


	<div class="ui special cards centered">
		<% if (locals.users[0]){
			locals.users.forEach(function(user) {  %>
			<%- include('elements/profile_card', {user: user}) %>
		<% }); } else { console.log("pas de user"); }%>
	</div>


<div class="ui centered grid">
    <div class="center aligned column">
		<% if (session.previous) {%>
		<a href="/research/?page=<%= session.previous %>">Previous</a>
		<% } %>
		<%= session.page %> / <% if (session.totalpage == 0) {%> 1 <% }else{ %><%= session.totalpage %><% } %>
		<% if (session.nextpage) {%>
		<a href="/research/?page=<%= session.nextpage %>">Next</a>
		<% } %>
	</div>
</div>

<script>
	$('.special.cards .image').dimmer({
		on: 'hover'
	});

</script>

<% include elements/footer %>
